# gitleaks fingerprint ignore file (https://github.com/gitleaks/gitleaks#gitleaksignore).
#
# .gitleaks.toml's [allowlist] regexes only apply to content matching --
# they can't reach a finding that's permanently baked into git history's
# immutable blobs. These are the exact commits where setup.sh's
# `MAXMIND_KEY=""` default (a false positive -- see the fix commit's own
# message for the full root-cause writeup) briefly existed before being
# fixed forward. Full-history gitleaks scans on push/tag events (see
# ci.yml's secret-scan job) will re-flag these on every single run
# forever otherwise, since the old commits themselves never change.
#
# Verified via a local `gitleaks detect --source . -v` against this exact
# state of master -- these are the only two fingerprints it reports, not a
# guess.
9c554b6d91d245a6f9735e51695c94c65f364df6:setup.sh:generic-api-key:135
1300d7dc8e1d93eaadbb6dda5e6dcd84f01e87cc:setup.sh:generic-api-key:135

# Same situation, a different fixture: test_settings.py's fake
# maxmind_license_key test value ("abcdefghij0123456789") tripped the
# same generic-api-key heuristic in this one commit before being swapped
# for an explicitly low-entropy fixture value (see that fix commit's own
# message). The old commit itself never changes, so it re-flags on every
# full-history scan otherwise.
97f6d76433fa1b5a7cd07117b3331a7896c871af:app/tests/test_settings.py:generic-api-key:106
97f6d76433fa1b5a7cd07117b3331a7896c871af:app/tests/test_settings.py:generic-api-key:114
97f6d76433fa1b5a7cd07117b3331a7896c871af:app/tests/test_settings.py:generic-api-key:118
97f6d76433fa1b5a7cd07117b3331a7896c871af:app/tests/test_settings.py:generic-api-key:130

# Same situation again: test_slack_notifications.py's Workflow Builder
# webhook regression test used a longer, more realistic-looking random
# token in this one commit before being swapped for an explicitly lower-
# entropy fixture (matching VALID_WEBHOOK's own shape) in the very next
# commit (0bc2a8c). The old commit itself never changes, so it re-flags on
# every full-history scan otherwise. Verified via a local `gitleaks
# detect --source . -v` -- the only fingerprint it reports against this
# exact state of master, not a guess.
12f4cb6298e36196affd9fc15107143c88db7004:app/tests/test_slack_notifications.py:slack-webhook-url:35
